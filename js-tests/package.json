{
  "name": "manage-students-ai-fixes-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:bug1": "jest bug1-hierarchy-unassigned --runInBand",
    "test:bug2": "jest bug2-ai-insights-double-init --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/*.test.js"]
  }
}
